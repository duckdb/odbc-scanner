# name: test/sql/db2/db2_version.test
# description: test for DB2 version
# group: [sql_db2_version]

require odbc_scanner

require-env DB2_AVAILABLE

statement ok
SET VARIABLE conn = odbc_connect('ODBCSCANNER_DEBUG_CONN_STRING_ENV_VAR=ODBC_CONN_STRING')

statement ok
CALL odbc_query(getvariable('conn'), 'SELECT SERVICE_LEVEL FROM SYSIBMADM.ENV_INST_INFO')

statement ok
SELECT odbc_close(getvariable('conn'))
