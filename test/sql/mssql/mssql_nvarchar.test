# name: test/sql/mssql/mssql_nvarchar.test
# description: test for MSSQL NVARCHAR type
# group: [sql_mssql_nvarchar]

require odbc_scanner

require-env MSSQL_AVAILABLE

statement ok
SET VARIABLE conn = odbc_connect('ODBCSCANNER_DEBUG_CONN_STRING_ENV_VAR=ODBC_CONN_STRING')

query I
SELECT * FROM odbc_query(getvariable('conn'), 'SELECT CAST(''foo'' AS NVARCHAR(MAX))')
----
foo

query I
SELECT column_type from (
  DESCRIBE SELECT * FROM odbc_query(getvariable('conn'), 'SELECT CAST(''foo'' AS NVARCHAR(MAX))')
)
----
VARCHAR

query I
SELECT * FROM odbc_query(getvariable('conn'), 'SELECT CAST(? AS VARCHAR(MAX))', params=row('🌌'::VARCHAR))
----
??

query I
SELECT * FROM odbc_query(getvariable('conn'), 'SELECT CAST(? AS NVARCHAR(MAX))', params=row('🌌'::VARCHAR))
----
🌌

statement ok
SET VARIABLE params1 = odbc_create_params()

statement ok
PREPARE p1 AS SELECT * FROM odbc_query(getvariable('conn'), 'SELECT CAST(? AS NVARCHAR(MAX))', params_handle=getvariable('params1'))

statement ok
PREPARE p1_bind AS SELECT odbc_bind_params(getvariable('conn'), getvariable('params1'), row(?))

statement ok
EXECUTE p1_bind('foo'::VARCHAR)

query I
EXECUTE p1
----
foo

statement ok
EXECUTE p1_bind('🌌'::VARCHAR)

query I
EXECUTE p1
----
🌌

statement ok
DEALLOCATE p1_bind

statement ok
DEALLOCATE p1

statement ok
SELECT odbc_close(getvariable('conn'))
